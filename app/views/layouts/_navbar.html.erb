<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#">Kreeti Article</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href=<%=root_path%>>Home</a>
      </li>
      <% if current_user.present? %>
        <li class="nav-item">
          <a class="nav-link" href=<%= user_dashboard_path %>>Dashboard</a>
        </li>
      <% end %>
    </ul>
    <form class="form-inline my-2 my-lg-0">
      <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
      <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
    </form>
    <% unless current_user.present? && ["new"].include?(params[:action])%>
      <ul class="navbar-nav ml-auto">
        <li class="nav-item text-center">
          <%= link_to "Login", new_session_path, class:"btn btn-success my-2 my-sm-0" %>
        </li>
        <li class="nav-item text-center">
          <%= link_to "Sign Up", new_user_path, class:"btn btn-success my-2 my-sm-0" %>
        </li>
      </ul>
    <% end %>
    <% if current_user.present? %>
      <ul class="navbar-nav ml-auto">
        <li class="nav-item text-center">
          <%= link_to "Logout", session_path(current_user), method: "delete", class:"btn btn-success my-2 my-sm-0" %>
        </li>
      </ul>
    <% end %>
  </div>
</nav>






